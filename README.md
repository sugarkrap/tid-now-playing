# What is this?

It's an Android app made for listening to what media is currently playing for then sending it in serial to another device that's plugged in.
Currently, it's only supported USB communication.

## How to build it

There's a ready to use script named dev.sh that takes care of everything. Otherwise, it's a Gradle project, you can still use `gradlew assembleDebug` to build a debug app.
